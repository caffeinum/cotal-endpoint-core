{
  "name": "@cotal-ai/endpoint-core",
  "version": "0.1.0",
  "description": "Channel-agnostic core for cotal mesh <-> chat-channel endpoints: routing, sticky targets, commands, the bridge orchestration, and the Transport/Formatter seams. Only @cotal-ai/core.",
  "type": "module",
  "license": "MIT",
  "author": "caffeinum",
  "repository": { "type": "git", "url": "git+https://github.com/caffeinum/cotal-endpoint-core.git" },
  "homepage": "https://github.com/caffeinum/cotal-endpoint-core#readme",
  "bugs": "https://github.com/caffeinum/cotal-endpoint-core/issues",
  "engines": { "node": ">=22" },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.ts",
    "check:core": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@cotal-ai/core": "^0.11.3"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "tsx": "4.22.4",
    "typescript": "5.9.3"
  }
}
